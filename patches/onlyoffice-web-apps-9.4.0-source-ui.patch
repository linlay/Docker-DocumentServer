--- a/apps/common/main/lib/view/Header.js
+++ b/apps/common/main/lib/view/Header.js
@@ -101,0 +102,9 @@
+                                '</div>' +
+                                '<% if (compactHeaderQuickAccess) { %>' +
+                                '<div class="hedset" role="menubar" aria-label="<%= scope.ariaQuickAccessToolbar %>">' +
+                                    '<div class="btn-slot" id="slot-btn-dt-save" data-layout-name="header-save"></div>' +
+                                    '<% if (compactHeaderQuickAccessPrint) { %>' +
+                                    '<div class="btn-slot" id="slot-btn-dt-print"></div>' +
+                                    '<% } %>' +
+                                    '<div class="btn-slot" id="slot-btn-dt-undo"></div>' +
+                                    '<div class="btn-slot" id="slot-btn-dt-redo"></div>' +
@@ -102,0 +112 @@
+                                '<% } %>' +
@@ -128,0 +139,3 @@
+                                    '<% if (compactHeaderHttpxIndicator) { %>' +
+                                    '<div class="btn-slot" id="slot-btn-dt-httpx-status" title="HTTPX" aria-label="HTTPX" style="display:none;position:relative;width:22px;height:40px;"><span aria-hidden="true" style="position:absolute;top:50%;left:50%;width:8px;height:8px;transform:translate(-50%,-50%);border-radius:50%;background:#35a56f;"></span></div>' +
+                                    '<% } %>' +
@@ -902 +915,5 @@
-                        textShare: this.textShare
+                        textShare: this.textShare,
+                        scope: me,
+                        compactHeaderQuickAccess: !!(config.compactHeader && config.customization && config.customization.compactHeaderQuickAccess),
+                        compactHeaderHttpxIndicator: !!(config.customization && config.customization.compactHeaderHttpxIndicator),
+                        compactHeaderQuickAccessPrint: !(config.customization && config.customization.compactHeaderQuickAccessPrint === false)
@@ -903,0 +921,21 @@
+
+                    var $httpxStatus = $html.find('#slot-btn-dt-httpx-status'),
+                        httpxParentOrigin = config.customization && config.customization.compactHeaderHttpxParentOrigin;
+                    if (me._httpxStatusMessageHandler) {
+                        window.removeEventListener('message', me._httpxStatusMessageHandler);
+                        me._httpxStatusMessageHandler = null;
+                    }
+                    if ($httpxStatus.length && typeof httpxParentOrigin === 'string' && httpxParentOrigin.length) {
+                        me._httpxStatusMessageHandler = function(event) {
+                            var data = event.data;
+                            if (event.source !== window.parent || event.origin !== httpxParentOrigin || !data ||
+                                data.source !== 'document-hub' || data.type !== 'httpx-relay-state')
+                                return;
+                            $httpxStatus.toggle(data.state === 'ready');
+                        };
+                        window.addEventListener('message', me._httpxStatusMessageHandler);
+                        window.parent.postMessage({
+                            source: 'onlyoffice-web-apps',
+                            type: 'httpx-relay-state-request'
+                        }, httpxParentOrigin);
+                    }
@@ -929,0 +968,41 @@
+                    if (config.compactHeader && config.customization && config.customization.compactHeaderQuickAccess) {
+                        if (config.canPrint && config.customization.compactHeaderQuickAccessPrint !== false) {
+                            me.btnPrint = createTitleButton('toolbar__icon icon--inverse btn-print', $html.findById('#slot-btn-dt-print'), true, undefined, undefined, 'P');
+                            me.shortcutHints.PrintPreviewAndPrint = {
+                                btn: me.btnPrint,
+                                label: me.tipPrint
+                            };
+                        } else {
+                            $html.find('#slot-btn-dt-print').hide();
+                        }
+
+                        if (config.showSaveButton) {
+                            let save_icon = config.canSaveToFile || config.isDesktopApp && config.isOffline ? 'btn-save' : 'btn-download';
+                            me.btnSave = createTitleButton('toolbar__icon icon--inverse ' + save_icon, $html.findById('#slot-btn-dt-save'), true, undefined, undefined, 'S');
+                            if (appConfig.canSaveToFile || appConfig.isDesktopApp && appConfig.isOffline) {
+                                me.shortcutHints.Save = {
+                                    btn: me.btnSave,
+                                    label: me.tipSave
+                                };
+                            } else {
+                                me.btnSave.updateHint(me.tipDownload);
+                            }
+                        } else {
+                            $html.find('#slot-btn-dt-save').hide();
+                        }
+
+                        me.btnUndo = createTitleButton('toolbar__icon icon--inverse btn-undo icon-rtl', $html.findById('#slot-btn-dt-undo'), true, undefined, undefined, 'Z',
+                                                        [Common.enumLock.undoLock, Common.enumLock.fileMenuOpened, Common.enumLock.lostConnect]);
+                        me.shortcutHints.EditUndo = {
+                            btn: me.btnUndo,
+                            label: me.tipUndo
+                        };
+
+                        me.btnRedo = createTitleButton('toolbar__icon icon--inverse btn-redo icon-rtl', $html.findById('#slot-btn-dt-redo'), true, undefined, undefined, 'Y',
+                                                        [Common.enumLock.redoLock, Common.enumLock.fileMenuOpened, Common.enumLock.lostConnect]);
+                        me.shortcutHints.EditRedo = {
+                            btn: me.btnRedo,
+                            label: me.tipRedo
+                        };
+                    }
+
--- a/apps/documenteditor/main/app/controller/Main.js
+++ b/apps/documenteditor/main/app/controller/Main.js
@@ -1804 +1804,3 @@
-                Common.UI.LayoutManager.init(this.editorConfig.customization ? this.editorConfig.customization.layout : null, this.appOptions.canBrandingExt, this.api);
+                this.appOptions.canSourceUiLayout = typeof this.editorConfig.customization == 'object' && this.editorConfig.customization.sourceUiLayout === true;
+                Common.UI.LayoutManager.init(this.editorConfig.customization ? this.editorConfig.customization.layout : null,
+                                              this.appOptions.canBrandingExt || this.appOptions.canSourceUiLayout, this.api);
@@ -2666,0 +2669,2 @@
+                    } else if (this.appOptions.canSourceUiLayout) {
+                        Common.UI.LayoutManager.applyCustomization();
--- a/apps/documenteditor/main/app/controller/Toolbar.js
+++ b/apps/documenteditor/main/app/controller/Toolbar.js
@@ -3922 +3922,3 @@
-            if ( Common.localStorage.itemExists(editmode ? "de-compact-toolbar" : "de-view-compact-toolbar") ) {
+            if (config.customization && config.customization.forceCompactToolbar) {
+                compactview = editmode ? !!config.customization.compactToolbar : config.customization.compactToolbar!==false;
+            } else if ( Common.localStorage.itemExists(editmode ? "de-compact-toolbar" : "de-view-compact-toolbar") ) {
@@ -3943 +3945 @@
-                if (!config.compactHeader) {
+                if (!config.compactHeader || config.customization && config.customization.compactHeaderQuickAccess) {
--- a/apps/documenteditor/main/app/controller/Viewport.js
+++ b/apps/documenteditor/main/app/controller/Viewport.js
@@ -82 +82,2 @@
-                        if (!config.twoLevelHeader || config.compactHeader)
+                        if ((!config.twoLevelHeader || config.compactHeader) &&
+                            !(config.customization && config.customization.compactHeaderHideLogo))
@@ -183,0 +185,2 @@
+            var forceCompactToolbar = config.customization && config.customization.forceCompactToolbar,
+                compactToolbar = config.customization && config.customization.compactToolbar;
@@ -186,2 +189,2 @@
-                ( !Common.localStorage.itemExists("de-compact-toolbar") &&
-                config.customization && config.customization.compactToolbar )) {
+                (forceCompactToolbar && compactToolbar) ||
+                (!forceCompactToolbar && !Common.localStorage.itemExists("de-compact-toolbar") && compactToolbar)) {
--- a/apps/spreadsheeteditor/main/app/controller/Main.js
+++ b/apps/spreadsheeteditor/main/app/controller/Main.js
@@ -1469 +1469,3 @@
-                    Common.UI.LayoutManager.init(this.editorConfig.customization ? this.editorConfig.customization.layout : null, this.appOptions.canBrandingExt, this.api);
+                    this.appOptions.canSourceUiLayout = typeof this.editorConfig.customization == 'object' && this.editorConfig.customization.sourceUiLayout === true;
+                    Common.UI.LayoutManager.init(this.editorConfig.customization ? this.editorConfig.customization.layout : null,
+                                                 this.appOptions.canBrandingExt || this.appOptions.canSourceUiLayout, this.api);
@@ -2549,0 +2552,2 @@
+                    } else if (this.appOptions.canSourceUiLayout) {
+                        Common.UI.LayoutManager.applyCustomization();
--- a/apps/spreadsheeteditor/main/app/controller/Toolbar.js
+++ b/apps/spreadsheeteditor/main/app/controller/Toolbar.js
@@ -4724 +4724,3 @@
-                if ( Common.localStorage.itemExists(editmode ? "sse-compact-toolbar" : "sse-view-compact-toolbar") ) {
+                if (config.customization && config.customization.forceCompactToolbar) {
+                    compactview = editmode ? !!config.customization.compactToolbar : config.customization.compactToolbar!==false;
+                } else if ( Common.localStorage.itemExists(editmode ? "sse-compact-toolbar" : "sse-view-compact-toolbar") ) {
@@ -4837 +4839 @@
-                    if (!config.compactHeader) {
+                    if (!config.compactHeader || config.customization && config.customization.compactHeaderQuickAccess) {
--- a/apps/spreadsheeteditor/main/app/controller/Viewport.js
+++ b/apps/spreadsheeteditor/main/app/controller/Viewport.js
@@ -89 +89,2 @@
-                        if (!config.twoLevelHeader || config.compactHeader)
+                        if ((!config.twoLevelHeader || config.compactHeader) &&
+                            !(config.customization && config.customization.compactHeaderHideLogo))
@@ -146,0 +148,3 @@
+
+            var forceCompactToolbar = config.customization && config.customization.forceCompactToolbar,
+                compactToolbar = config.customization && config.customization.compactToolbar;
@@ -149,2 +153,2 @@
-                    ( !Common.localStorage.itemExists("sse-compact-toolbar") &&
-                        config.customization && config.customization.compactToolbar ))
+                    (forceCompactToolbar && compactToolbar) ||
+                    (!forceCompactToolbar && !Common.localStorage.itemExists("sse-compact-toolbar") && compactToolbar))
--- a/apps/presentationeditor/main/app/controller/Main.js
+++ b/apps/presentationeditor/main/app/controller/Main.js
@@ -1404 +1404,3 @@
-                Common.UI.LayoutManager.init(this.editorConfig.customization ? this.editorConfig.customization.layout : null, this.appOptions.canBrandingExt, this.api);
+                this.appOptions.canSourceUiLayout = typeof this.editorConfig.customization == 'object' && this.editorConfig.customization.sourceUiLayout === true;
+                Common.UI.LayoutManager.init(this.editorConfig.customization ? this.editorConfig.customization.layout : null,
+                                              this.appOptions.canBrandingExt || this.appOptions.canSourceUiLayout, this.api);
@@ -2089,0 +2092,2 @@
+                    } else if (this.appOptions.canSourceUiLayout) {
+                        Common.UI.LayoutManager.applyCustomization();
--- a/apps/presentationeditor/main/app/controller/Toolbar.js
+++ b/apps/presentationeditor/main/app/controller/Toolbar.js
@@ -309,7 +309,5 @@
                 'tipChartTab' : {name: 'pe-help-tip-chart-tab', placement: 'bottom', text: this.helpChartTab, header: this.helpChartTabHeader, target: 'li.ribtab #charttab', maxwidth: 300,
                     automove: true, closable: false, isNewFeature: true},
-                'gifPlayback' : {name:'pe-help-tip-gif-payback', placement: 'bottom', text: this.helpGifPlayback, header: this.helpGifPlaybackHeader, target: '#toolbar', maxwidth: 300,
-                    automove: true, noArrow: true, noHighlight: true, closable: false, isNewFeature: true},
                 'slideTransitions' : {name:'pe-help-tip-slideTransitions', placement: 'bottom', text: this.helpSlideTransitions, header: this.helpSlideTransitionsHeader, target: '#transit-field-effects', maxwidth: 300,
                     automove: true, closable: false, isNewFeature: true},
                 'slideTheme' : {name:'pe-help-tip-slideTheme', placement: 'bottom', text: this.helpSlideTheme, header: this.helpSlideThemeHeader, target: '#slot-field-styles', maxwidth: 300,
@@ -2805 +2805,3 @@
-            if ( Common.localStorage.itemExists(editmode ? "pe-compact-toolbar" : "pe-view-compact-toolbar") ) {
+            if (config.customization && config.customization.forceCompactToolbar) {
+                compactview = editmode ? !!config.customization.compactToolbar : config.customization.compactToolbar!==false;
+            } else if ( Common.localStorage.itemExists(editmode ? "pe-compact-toolbar" : "pe-view-compact-toolbar") ) {
@@ -2872 +2874 @@
-                if (!config.compactHeader) {
+                if (!config.compactHeader || config.customization && config.customization.compactHeaderQuickAccess) {
--- a/apps/presentationeditor/main/app/controller/Viewport.js
+++ b/apps/presentationeditor/main/app/controller/Viewport.js
@@ -83 +83,2 @@
-                        if (!config.twoLevelHeader || config.compactHeader)
+                        if ((!config.twoLevelHeader || config.compactHeader) &&
+                            !(config.customization && config.customization.compactHeaderHideLogo))
@@ -186,0 +188,3 @@
+
+            var forceCompactToolbar = config.customization && config.customization.forceCompactToolbar,
+                compactToolbar = config.customization && config.customization.compactToolbar;
@@ -189,2 +193,2 @@
-                ( !Common.localStorage.itemExists("pe-compact-toolbar") &&
-                    config.customization && config.customization.compactToolbar ))
+                (forceCompactToolbar && compactToolbar) ||
+                (!forceCompactToolbar && !Common.localStorage.itemExists("pe-compact-toolbar") && compactToolbar))
