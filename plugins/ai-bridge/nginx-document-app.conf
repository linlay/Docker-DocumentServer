# document-hub v1 owns every business endpoint.
#
# Intentionally do not expose the legacy UUID document routes, /new-*, /admin,
# /documents/editor, document storage, or /copilot-api through DocumentServer.
# The Python Relay listens on container port 3001 and is reachable only from the
# private Docker network. document-hub authenticates and proxies every request.
